PORT=4000
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=change-me-to-a-long-random-string-at-least-64-chars
ADMIN_EMAIL=hi@hieu.dev
ADMIN_PASSWORD=change-me
COOKIE_SECURE=false

# Firestore — pick ONE mode:
#  1) emulator (dev)
FIRESTORE_EMULATOR_HOST=localhost:8080
#  2) cloud, file path (recommended for local dev w/ real Firestore)
#FIREBASE_SERVICE_ACCOUNT_FILE=./secrets/firebase-service-account.json
#  3) cloud, inline JSON (for hosted prod where you can't upload files)
#FIREBASE_SERVICE_ACCOUNT_JSON=

# Cloudinary
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=hieublog-dev
