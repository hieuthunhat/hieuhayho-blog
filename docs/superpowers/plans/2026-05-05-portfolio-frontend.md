# Portfolio Frontend Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the frontend of HieuBlog (homepage, blogs list + detail, contact) per the approved spec at `docs/superpowers/specs/2026-05-05-portfolio-frontend-design.md`. All data flows through a typed `api` module that serves mock data locally and will swap to a real Koa backend later via `VITE_API_BASE`.

**Architecture:** Vite + React 18 + TypeScript SPA, react-router v7 for routing, shadcn/ui components on Tailwind v4. A single `src/lib/api.ts` is the only seam for data — pages never call `fetch` directly. While the backend is offline, `api.ts` re-exports `mock-api.ts`; once `VITE_API_BASE` is set, it makes real `fetch` calls. Background dot-grid is applied once on `<body>` so it stays continuous across route changes.

**Tech Stack:** Vite, React 18, TypeScript, Tailwind v4, shadcn/ui, lucide-react, react-router-dom v7, react-markdown + remark-gfm, @tailwindcss/typography, vitest + @testing-library/react.

---

## Progress log

| # | Task | Status | Notes |
|---|---|---|---|
| 1 | Reset client/ + scaffold Vite+React+TS | ✅ done | Branch `worktree-portfolio-frontend`, commits `f38aff8` (scaffold) + `9492ccc` (gitignore tsbuildinfo). Spec ✅ and code-quality reviews APPROVED WITH NITS. |
| 2 | Tailwind v4 + dot grid | ⏳ next | — |
| 3 | shadcn/ui init | ⏳ pending | — |
| 4 | Vitest + RTL | ⏳ pending | — |
| 5 | Types + mock API + seeds | ⏳ pending | — |
| 6 | App shell + routing | ⏳ pending | — |
| 7 | Homepage components | ⏳ pending | — |
| 8 | Blogs list | ⏳ pending | — |
| 9 | Blog detail | ⏳ pending | — |
| 10 | Contact page | ⏳ pending | — |
| 11 | Smoke check | ⏳ pending | — |

### Open nits from Task 1 (apply during Task 2 when those files are touched)

- **I2** — Add `noUnusedLocals` and `noUnusedParameters` to `client/tsconfig.node.json` to match `tsconfig.app.json` strictness. The Tailwind plugin import in Task 2 is a natural moment to harden this file.
- **I3** — Pin `@types/node` to `^22.0.0` (active LTS) instead of the current `^25.6.0`. Edit `client/package.json` and re-run `npm install`.

### Where to resume

- **Worktree:** `/Users/avada/WebstormProjects/HieuBlog/.claude/worktrees/portfolio-frontend`
- **Branch:** `worktree-portfolio-frontend` (created from master at `e04cd91`)
- **Next action:** Begin Task 2 (Tailwind v4 + dot-grid background). Skill flow: `superpowers:subagent-driven-development` is the active skill; dispatch a fresh implementer subagent per task with two-stage review (spec then code quality).

---

## File Map

**Wipe (replace contents):**
- `client/src/main.js` → becomes `main.tsx`
- `client/src/counter.js` → delete
- `client/src/style.css` → delete (replaced by `globals.css`)
- `client/src/assets/{hero.png,javascript.svg,vite.svg}` → delete
- `client/index.html` → modify (root id `app` → `root`, title)
- `client/package.json` → rewritten

**Create — config:**
- `client/tsconfig.json`, `client/tsconfig.node.json`, `client/tsconfig.app.json`
- `client/vite.config.ts`
- `client/components.json` (shadcn config)
- `client/.env.example`

**Create — source:**
- `client/src/main.tsx` — bootstraps React + RouterProvider
- `client/src/App.tsx` — `<Nav/> <Outlet/> <Footer/>` shell
- `client/src/globals.css` — Tailwind v4 import + dot-grid background + theme tokens
- `client/src/test-setup.ts` — Vitest + jest-dom

- `client/src/lib/types.ts` — `Post`, `PostSummary`, `ContactPayload`, `ApiError`
- `client/src/lib/utils.ts` — shadcn `cn()` helper
- `client/src/lib/api.ts` — public surface; switches between mock and real
- `client/src/lib/mock-api.ts` — in-memory mock implementation
- `client/src/lib/__tests__/mock-api.test.ts`

- `client/src/content/seed-posts.ts` — 4 mock blog posts with markdown bodies
- `client/src/content/seed-projects.ts` — 4 homepage project cards

- `client/src/routes/home.tsx`
- `client/src/routes/blogs.tsx`
- `client/src/routes/blog-detail.tsx`
- `client/src/routes/contact.tsx`
- `client/src/routes/not-found.tsx`

- `client/src/components/layout/nav.tsx`
- `client/src/components/layout/footer.tsx`
- `client/src/components/layout/__tests__/nav.test.tsx`

- `client/src/components/home/hero.tsx`
- `client/src/components/home/stats.tsx`
- `client/src/components/home/work-grid.tsx`
- `client/src/components/home/tools-card.tsx`
- `client/src/components/home/contact-cta.tsx`
- `client/src/components/home/__tests__/work-grid.test.tsx`

- `client/src/components/blog/post-card.tsx`
- `client/src/components/blog/post-meta.tsx`
- `client/src/components/blog/__tests__/post-card.test.tsx`

- `client/src/components/ui/*` — generated by shadcn

- `client/src/routes/__tests__/blogs.test.tsx`
- `client/src/routes/__tests__/blog-detail.test.tsx`
- `client/src/routes/__tests__/contact.test.tsx`

---

## Working directory note

All commands in this plan run from `client/` unless explicitly stated otherwise. From the repo root, prefix with `cd client && ...` or run them in a shell that's already in `client/`.

---

## Task 1: Reset client/ and scaffold Vite + React + TypeScript

**Files:**
- Delete: `client/src/main.js`, `client/src/counter.js`, `client/src/style.css`, `client/src/assets/{hero.png,javascript.svg,vite.svg}`
- Modify: `client/package.json`, `client/index.html`
- Create: `client/src/main.tsx`, `client/src/App.tsx`, `client/tsconfig.json`, `client/tsconfig.app.json`, `client/tsconfig.node.json`, `client/vite.config.ts`

- [ ] **Step 1: Delete vanilla scaffold files**

```bash
cd client
rm -f src/main.js src/counter.js src/style.css
rm -f src/assets/hero.png src/assets/javascript.svg src/assets/vite.svg
rmdir src/assets 2>/dev/null || true
```

- [ ] **Step 2: Replace `client/package.json`**

```json
{
  "name": "client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
```

Note: Vite 5 (not 8 from the original scaffold) — Vite 5 is the current stable line widely supported by the React + shadcn ecosystem. We'll add Tailwind, shadcn, and test deps in later tasks rather than dumping everything in now.

- [ ] **Step 3: Install dependencies**

Run: `npm install`
Expected: completes without errors; `node_modules/` populated.

- [ ] **Step 4: Replace `client/index.html`**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>hieu.dev</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 5: Create `client/tsconfig.json`**

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

- [ ] **Step 6: Create `client/tsconfig.app.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"]
}
```

- [ ] **Step 7: Create `client/tsconfig.node.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "noEmit": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 8: Create `client/vite.config.ts`**

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'node:path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
});
```

- [ ] **Step 9: Create `client/src/main.tsx`**

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './App';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

- [ ] **Step 10: Create `client/src/App.tsx` (placeholder, no router yet)**

```tsx
export default function App() {
  return <div>hieu.dev — bootstrapping</div>;
}
```

- [ ] **Step 11: Verify dev server boots**

Run: `npm run dev`
Expected: Vite prints a local URL (e.g. `http://localhost:5173/`). Open it; the page shows "hieu.dev — bootstrapping". Stop the server with Ctrl-C.

- [ ] **Step 12: Verify build**

Run: `npm run build`
Expected: completes without TypeScript errors; produces `client/dist/`.

- [ ] **Step 13: Commit**

```bash
git add client/
git commit -m "chore(client): reset scaffold to Vite + React + TypeScript"
```

---

## Task 2: Tailwind v4 + dot-grid background

**Files:**
- Modify: `client/package.json`, `client/vite.config.ts`
- Create: `client/src/globals.css`
- Modify: `client/src/main.tsx` (import globals.css)

- [ ] **Step 1: Install Tailwind v4 + Vite plugin**

Run: `npm install -D tailwindcss@next @tailwindcss/vite@next`
Expected: installs successfully. (`@next` ensures we're on the v4 line; pin to `^4.0.0` once stable v4 is the default.)

- [ ] **Step 2: Wire the Tailwind Vite plugin into `client/vite.config.ts`**

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import path from 'node:path';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
});
```

- [ ] **Step 3: Create `client/src/globals.css`**

```css
@import "tailwindcss";

@theme {
  --color-blueprint-bg: #ffffff;
  --color-blueprint-dot: #1d4ed8;
}

body {
  background-color: var(--color-blueprint-bg);
  background-image: radial-gradient(circle, var(--color-blueprint-dot) 1.2px, transparent 1.2px);
  background-size: 32px 32px;
  background-position: 0 0;
  min-height: 100vh;
  font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
  -webkit-font-smoothing: antialiased;
  color: #172554; /* blue-950 */
}
```

- [ ] **Step 4: Import `globals.css` in `client/src/main.tsx`**

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import './globals.css';
import App from './App';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

- [ ] **Step 5: Update `client/src/App.tsx` to verify Tailwind is wired**

```tsx
export default function App() {
  return (
    <div className="mx-auto max-w-5xl p-8">
      <h1 className="text-2xl font-medium text-blue-950">hieu.dev</h1>
      <p className="text-sm text-blue-700">If you can read this in blue with a dotted background, Tailwind + the dot grid are wired.</p>
    </div>
  );
}
```

- [ ] **Step 6: Verify in browser**

Run: `npm run dev`. Open the URL. Expected:
- White background with blue dots in a 32px grid.
- Heading in `blue-950`, subtext in `blue-700`.
- Container is centered, max-width ~1024px with side padding.

Stop the server.

- [ ] **Step 7: Commit**

```bash
git add client/
git commit -m "chore(client): add Tailwind v4 and dot-grid background"
```

---

## Task 3: Initialize shadcn/ui + add base components

**Files:**
- Create: `client/components.json`, `client/src/lib/utils.ts`, `client/src/components/ui/*` (generated)
- Modify: `client/src/globals.css` (shadcn theme tokens), `client/package.json` (deps added by CLI)

- [ ] **Step 1: Initialize shadcn/ui**

Run: `npx shadcn@latest init`
- When asked about the base color, choose `neutral` (or `slate`).
- When asked about CSS variables, accept defaults (yes).
- The CLI will create `components.json`, `src/lib/utils.ts`, and update `globals.css` with theme tokens.

Expected: `components.json` exists; `src/lib/utils.ts` exports `cn(...)`; `globals.css` has shadcn theme tokens added.

- [ ] **Step 2: Re-apply our dot-grid background to body**

The shadcn init may have replaced our `body` rule. Open `client/src/globals.css` and ensure the body rule from Task 2 Step 3 is still present. If shadcn replaced it, append it back at the bottom of the file:

```css
body {
  background-color: #ffffff;
  background-image: radial-gradient(circle, #1d4ed8 1.2px, transparent 1.2px);
  background-size: 32px 32px;
  background-position: 0 0;
  min-height: 100vh;
}
```

- [ ] **Step 3: Add the shadcn components used across the app**

Run:
```bash
npx shadcn@latest add button card badge separator avatar input skeleton sonner textarea navigation-menu
```

Expected: each component file lands in `client/src/components/ui/`. Required peer deps (Radix primitives, class-variance-authority, lucide-react, sonner) are added automatically.

- [ ] **Step 4: Verify a Button renders correctly**

Update `client/src/App.tsx`:

```tsx
import { Button } from '@/components/ui/button';

export default function App() {
  return (
    <div className="mx-auto max-w-5xl p-8">
      <h1 className="text-2xl font-medium text-blue-950">hieu.dev</h1>
      <Button className="mt-4 bg-blue-700 hover:bg-blue-800">test button</Button>
    </div>
  );
}
```

Run `npm run dev`, open the URL. Expected: blue-700 button renders, hover darkens to blue-800. Stop the server.

- [ ] **Step 5: Commit**

```bash
git add client/
git commit -m "chore(client): init shadcn/ui and add base components"
```

---

## Task 4: Vitest + React Testing Library setup

**Files:**
- Modify: `client/package.json`, `client/vite.config.ts`, `client/tsconfig.app.json`
- Create: `client/src/test-setup.ts`

- [ ] **Step 1: Install test dependencies**

Run:
```bash
npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom @vitest/ui
```

- [ ] **Step 2: Create `client/src/test-setup.ts`**

```ts
import '@testing-library/jest-dom/vitest';
import { afterEach } from 'vitest';
import { cleanup } from '@testing-library/react';

afterEach(() => {
  cleanup();
});
```

- [ ] **Step 3: Update `client/vite.config.ts` to add the Vitest config**

```ts
/// <reference types="vitest" />
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import path from 'node:path';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test-setup.ts'],
    css: true,
  },
});
```

- [ ] **Step 4: Add Vitest globals to `tsconfig.app.json`**

In `client/tsconfig.app.json`, add `"vitest/globals"` to `compilerOptions.types`. If `types` doesn't exist, create it:

```json
{
  "compilerOptions": {
    ...,
    "types": ["vitest/globals"]
  },
  "include": ["src"]
}
```

- [ ] **Step 5: Write a sanity test**

Create `client/src/lib/__tests__/sanity.test.ts`:

```ts
describe('sanity', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
```

- [ ] **Step 6: Run the sanity test**

Run: `npm run test:run`
Expected: 1 passing test.

- [ ] **Step 7: Delete the sanity test**

```bash
rm client/src/lib/__tests__/sanity.test.ts
```

- [ ] **Step 8: Commit**

```bash
git add client/
git commit -m "chore(client): add Vitest + React Testing Library"
```

---

## Task 5: Types, mock API, and seed data (TDD)

**Files:**
- Create: `client/src/lib/types.ts`, `client/src/lib/mock-api.ts`, `client/src/lib/api.ts`, `client/src/content/seed-posts.ts`, `client/src/content/seed-projects.ts`, `client/src/lib/__tests__/mock-api.test.ts`

- [ ] **Step 1: Create `client/src/lib/types.ts`**

```ts
export type Post = {
  slug: string;
  title: string;
  excerpt: string;
  content: string;
  publishedAt: string;
  readingMinutes: number;
  tags: string[];
};

export type PostSummary = Omit<Post, 'content'>;

export type ContactPayload = {
  name: string;
  email: string;
  message: string;
};

export type ApiError = {
  message: string;
  status: number;
};

export type Project = {
  name: string;
  type: string;
  year: string;
  bg: string;
  accent: string;
  shape: 'square' | 'circle' | 'parallelogram';
  tags: string[];
};
```

- [ ] **Step 2: Create `client/src/content/seed-projects.ts`**

```ts
import type { Project } from '@/lib/types';

export const seedProjects: Project[] = [
  {
    name: 'Fenway Banking',
    type: 'Mobile redesign',
    year: '2025',
    bg: '#E6F1FB',
    accent: '#185FA5',
    shape: 'square',
    tags: ['fintech', 'mobile'],
  },
  {
    name: 'Pulse Analytics',
    type: 'Dashboard system',
    year: '2025',
    bg: '#FAECE7',
    accent: '#D85A30',
    shape: 'parallelogram',
    tags: ['b2b', 'data viz'],
  },
  {
    name: 'Verde wellness',
    type: 'Brand & web',
    year: '2025',
    bg: '#E1F5EE',
    accent: '#1D9E75',
    shape: 'circle',
    tags: ['brand', 'web'],
  },
  {
    name: 'Lumen design system',
    type: 'Internal tooling',
    year: 'ongoing',
    bg: '#EEEDFE',
    accent: '#534AB7',
    shape: 'square',
    tags: ['systems', 'tokens'],
  },
];
```

- [ ] **Step 3: Create `client/src/content/seed-posts.ts`**

```ts
import type { Post } from '@/lib/types';

export const seedPosts: Post[] = [
  {
    slug: 'calm-interfaces',
    title: 'On building calm interfaces',
    excerpt: 'Why subtraction is the most underrated tool in interface design.',
    publishedAt: '2026-04-12',
    readingMinutes: 4,
    tags: ['design', 'craft'],
    content: `# On building calm interfaces

The best interfaces ask less of you. They reveal what matters,
hide what doesn't, and trust that you'll find your way.

## A short list

- Fewer colors
- Fewer animations
- Fewer modes

When in doubt, remove a thing.`,
  },
  {
    slug: 'shipping-small',
    title: 'Shipping small, often',
    excerpt: 'A note on cadence over scope.',
    publishedAt: '2026-03-02',
    readingMinutes: 3,
    tags: ['process'],
    content: `# Shipping small, often

Big releases are mostly an accounting trick. Real progress
compounds in small daily moves.`,
  },
  {
    slug: 'blueprint-aesthetic',
    title: 'The blueprint aesthetic',
    excerpt: 'Dot grids, blue ink, and why technical drawings still feel modern.',
    publishedAt: '2026-02-18',
    readingMinutes: 5,
    tags: ['design', 'aesthetics'],
    content: `# The blueprint aesthetic

Engineering drawings have a quiet confidence: nothing decorative,
everything load-bearing. That feels relevant again.`,
  },
  {
    slug: 'koa-notes',
    title: 'Koa notes — small server, big mileage',
    excerpt: 'Why Koa keeps showing up in my side projects.',
    publishedAt: '2026-01-09',
    readingMinutes: 6,
    tags: ['backend', 'node'],
    content: `# Koa notes

Koa is small enough to read in a sitting and gives you exactly
the seams you need. Most "frameworks" hide too much.`,
  },
];
```

- [ ] **Step 4: Write the failing test for `mock-api.ts`**

Create `client/src/lib/__tests__/mock-api.test.ts`:

```ts
import { mockApi } from '@/lib/mock-api';

describe('mockApi.posts.list', () => {
  it('returns post summaries without the full content field', async () => {
    const posts = await mockApi.posts.list();
    expect(posts.length).toBeGreaterThan(0);
    posts.forEach((p) => {
      expect(p).toHaveProperty('slug');
      expect(p).toHaveProperty('title');
      expect(p).toHaveProperty('excerpt');
      expect(p).not.toHaveProperty('content');
    });
  });

  it('returns posts in reverse chronological order (newest first)', async () => {
    const posts = await mockApi.posts.list();
    for (let i = 0; i < posts.length - 1; i++) {
      expect(posts[i].publishedAt >= posts[i + 1].publishedAt).toBe(true);
    }
  });
});

describe('mockApi.posts.get', () => {
  it('returns the post matching the slug, with full content', async () => {
    const post = await mockApi.posts.get('calm-interfaces');
    expect(post).not.toBeNull();
    expect(post?.slug).toBe('calm-interfaces');
    expect(post?.content).toContain('calm interfaces');
  });

  it('returns null for an unknown slug', async () => {
    const post = await mockApi.posts.get('does-not-exist');
    expect(post).toBeNull();
  });
});

describe('mockApi.contact.send', () => {
  it('resolves { ok: true } for a valid payload', async () => {
    const result = await mockApi.contact.send({
      name: 'Hieu',
      email: 'hi@hieu.dev',
      message: 'hello world from a unit test',
    });
    expect(result).toEqual({ ok: true });
  });

  it('rejects with ApiError when email is empty', async () => {
    await expect(
      mockApi.contact.send({ name: 'x', email: '', message: 'long enough message' })
    ).rejects.toMatchObject({ status: 400 });
  });

  it('rejects with ApiError when message is shorter than 10 chars', async () => {
    await expect(
      mockApi.contact.send({ name: 'x', email: 'x@x.com', message: 'short' })
    ).rejects.toMatchObject({ status: 400 });
  });
});
```

- [ ] **Step 5: Run the test and verify it fails**

Run: `npm run test:run`
Expected: All `mockApi.*` tests fail because `mock-api.ts` doesn't exist yet.

- [ ] **Step 6: Implement `client/src/lib/mock-api.ts`**

```ts
import type { ContactPayload, Post, PostSummary, ApiError } from '@/lib/types';
import { seedPosts } from '@/content/seed-posts';

const delay = (ms = 300) => new Promise((r) => setTimeout(r, ms));

const toSummary = (post: Post): PostSummary => ({
  slug: post.slug,
  title: post.title,
  excerpt: post.excerpt,
  publishedAt: post.publishedAt,
  readingMinutes: post.readingMinutes,
  tags: post.tags,
});

export const mockApi = {
  posts: {
    async list(): Promise<PostSummary[]> {
      await delay();
      return [...seedPosts]
        .sort((a, b) => (a.publishedAt < b.publishedAt ? 1 : -1))
        .map(toSummary);
    },

    async get(slug: string): Promise<Post | null> {
      await delay();
      const found = seedPosts.find((p) => p.slug === slug);
      return found ?? null;
    },
  },

  contact: {
    async send(payload: ContactPayload): Promise<{ ok: true }> {
      await delay();
      if (!payload.email.trim()) {
        const err: ApiError = { message: 'Email is required', status: 400 };
        throw err;
      }
      if (payload.message.trim().length < 10) {
        const err: ApiError = { message: 'Message must be at least 10 characters', status: 400 };
        throw err;
      }
      return { ok: true };
    },
  },
};
```

- [ ] **Step 7: Run tests and verify they pass**

Run: `npm run test:run`
Expected: All `mockApi.*` tests pass.

- [ ] **Step 8: Create `client/src/lib/api.ts` (the public surface that switches mock vs. real)**

```ts
import type { ContactPayload, Post, PostSummary } from '@/lib/types';
import { mockApi } from '@/lib/mock-api';

const BASE = import.meta.env.VITE_API_BASE?.trim();

const usingMock = !BASE;

async function http<T>(path: string, init?: RequestInit): Promise<T> {
  const res = await fetch(`${BASE}${path}`, {
    headers: { 'content-type': 'application/json', ...(init?.headers ?? {}) },
    ...init,
  });
  if (!res.ok) {
    const body = await res.json().catch(() => ({ message: res.statusText }));
    throw { message: body.message ?? 'Request failed', status: res.status };
  }
  return res.json() as Promise<T>;
}

export const api = usingMock
  ? mockApi
  : {
      posts: {
        list: () => http<PostSummary[]>('/api/posts'),
        get: async (slug: string): Promise<Post | null> => {
          try {
            return await http<Post>(`/api/posts/${encodeURIComponent(slug)}`);
          } catch (e) {
            if ((e as { status?: number }).status === 404) return null;
            throw e;
          }
        },
      },
      contact: {
        send: (payload: ContactPayload) =>
          http<{ ok: true }>('/api/contact', {
            method: 'POST',
            body: JSON.stringify(payload),
          }),
      },
    };
```

- [ ] **Step 9: Create `client/.env.example`**

```
# Leave VITE_API_BASE empty (or unset) to use the in-memory mock API.
# Set it to your Koa backend URL once that phase begins, e.g.:
# VITE_API_BASE=http://localhost:4000
VITE_API_BASE=
```

- [ ] **Step 10: Run all tests**

Run: `npm run test:run`
Expected: all tests pass.

- [ ] **Step 11: Commit**

```bash
git add client/
git commit -m "feat(client): add types, mock API, seed data, and api.ts switch"
```

---

## Task 6: App shell (router + Nav + Footer) with TDD on Nav

**Files:**
- Modify: `client/src/App.tsx`, `client/src/main.tsx`
- Create: `client/src/components/layout/nav.tsx`, `client/src/components/layout/footer.tsx`, `client/src/components/layout/__tests__/nav.test.tsx`
- Create stub routes: `client/src/routes/{home,blogs,blog-detail,contact,not-found}.tsx`

- [ ] **Step 1: Create stub route files (one card each, placeholder copy)**

`client/src/routes/home.tsx`:
```tsx
export default function Home() {
  return <div className="text-blue-950">home (placeholder)</div>;
}
```

`client/src/routes/blogs.tsx`:
```tsx
export default function Blogs() {
  return <div className="text-blue-950">blogs (placeholder)</div>;
}
```

`client/src/routes/blog-detail.tsx`:
```tsx
import { useParams } from 'react-router-dom';

export default function BlogDetail() {
  const { slug } = useParams<{ slug: string }>();
  return <div className="text-blue-950">blog detail: {slug} (placeholder)</div>;
}
```

`client/src/routes/contact.tsx`:
```tsx
export default function Contact() {
  return <div className="text-blue-950">contact (placeholder)</div>;
}
```

`client/src/routes/not-found.tsx`:
```tsx
import { Link } from 'react-router-dom';
import { Card, CardContent } from '@/components/ui/card';
import { Button } from '@/components/ui/button';

export default function NotFound() {
  return (
    <Card className="mt-12">
      <CardContent className="flex flex-col items-center gap-4 p-10 text-center">
        <p className="text-base text-blue-950">This page doesn't exist.</p>
        <Button asChild className="bg-blue-700 hover:bg-blue-800">
          <Link to="/">back home</Link>
        </Button>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 2: Create `client/src/components/layout/footer.tsx`**

```tsx
export default function Footer() {
  return (
    <footer className="mt-12 pb-8 text-center text-xs text-muted-foreground">
      © 2026 hieu.dev · made with care
    </footer>
  );
}
```

- [ ] **Step 3: Write the failing test for `Nav`**

Create `client/src/components/layout/__tests__/nav.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import Nav from '@/components/layout/nav';

function renderNav(initialEntries = ['/']) {
  return render(
    <MemoryRouter initialEntries={initialEntries}>
      <Nav />
    </MemoryRouter>
  );
}

describe('<Nav />', () => {
  it('renders the brand text "hieu.dev" and the logo letter "H"', () => {
    renderNav();
    expect(screen.getByText('hieu.dev')).toBeInTheDocument();
    expect(screen.getByText('H')).toBeInTheDocument();
  });

  it('renders three nav links: work, writing, contact', () => {
    renderNav();
    expect(screen.getByRole('link', { name: 'work' })).toBeInTheDocument();
    expect(screen.getByRole('link', { name: 'writing' })).toBeInTheDocument();
    expect(screen.getByRole('link', { name: 'contact' })).toBeInTheDocument();
  });

  it('marks the writing link as current when on /blogs', () => {
    renderNav(['/blogs']);
    const writing = screen.getByRole('link', { name: 'writing' });
    expect(writing).toHaveAttribute('aria-current', 'page');
  });
});
```

- [ ] **Step 4: Run the test and verify it fails**

Run: `npm run test:run -- nav`
Expected: tests fail because `Nav` does not exist.

- [ ] **Step 5: Implement `client/src/components/layout/nav.tsx`**

```tsx
import { Link, NavLink } from 'react-router-dom';
import { Avatar, AvatarFallback } from '@/components/ui/avatar';
import { cn } from '@/lib/utils';

const links = [
  { to: '/#work', label: 'work', match: '/' as const },
  { to: '/blogs', label: 'writing', match: '/blogs' as const },
  { to: '/contact', label: 'contact', match: '/contact' as const },
];

export default function Nav() {
  return (
    <nav className="mx-auto mb-12 flex max-w-5xl items-center justify-between px-8 pt-8">
      <Link to="/" className="flex items-center gap-2.5">
        <Avatar className="h-8 w-8 rounded-md">
          <AvatarFallback className="rounded-md bg-blue-700 text-sm font-medium text-white">
            H
          </AvatarFallback>
        </Avatar>
        <span className="text-sm font-medium text-blue-950">hieu.dev</span>
      </Link>

      <ul className="flex gap-6 text-sm text-blue-900">
        {links.map((l) => {
          const isAnchor = l.to.includes('#');
          if (isAnchor) {
            return (
              <li key={l.label}>
                <a href={l.to} className="hover:underline">
                  {l.label}
                </a>
              </li>
            );
          }
          return (
            <li key={l.label}>
              <NavLink
                to={l.to}
                className={({ isActive }) =>
                  cn('hover:underline', isActive && 'underline')
                }
              >
                {l.label}
              </NavLink>
            </li>
          );
        })}
      </ul>
    </nav>
  );
}
```

Note on `aria-current`: react-router's `NavLink` automatically sets `aria-current="page"` when active, which is what the test asserts. The "work" link is a plain `<a>` because it's a same-page anchor — no active styling needed.

- [ ] **Step 6: Run the test and verify it passes**

Run: `npm run test:run -- nav`
Expected: all `<Nav />` tests pass.

- [ ] **Step 7: Replace `client/src/App.tsx` with the router shell**

```tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Nav from '@/components/layout/nav';
import Footer from '@/components/layout/footer';
import Home from '@/routes/home';
import Blogs from '@/routes/blogs';
import BlogDetail from '@/routes/blog-detail';
import Contact from '@/routes/contact';
import NotFound from '@/routes/not-found';

export default function App() {
  return (
    <BrowserRouter>
      <Nav />
      <main className="mx-auto max-w-5xl px-8 pb-16">
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/blogs" element={<Blogs />} />
          <Route path="/blogs/:slug" element={<BlogDetail />} />
          <Route path="/contact" element={<Contact />} />
          <Route path="*" element={<NotFound />} />
        </Routes>
      </main>
      <Footer />
    </BrowserRouter>
  );
}
```

- [ ] **Step 8: Manually verify routing in the browser**

Run: `npm run dev`. In the browser:
- `/` shows the home placeholder.
- `/blogs` shows the blogs placeholder.
- `/blogs/calm-interfaces` shows the detail placeholder with the slug.
- `/contact` shows the contact placeholder.
- `/asdfgh` shows the 404 card.
- Clicking nav links navigates without a full reload.
- Footer is visible at the bottom of every page.
- Dot grid is continuous across navigations.

Stop the server.

- [ ] **Step 9: Run all tests**

Run: `npm run test:run`
Expected: all tests pass.

- [ ] **Step 10: Commit**

```bash
git add client/
git commit -m "feat(client): add router shell with Nav, Footer, and stub routes"
```

---

## Task 7: Homepage components (TDD on WorkGrid)

**Files:**
- Create: `client/src/components/home/{hero,stats,work-grid,tools-card,contact-cta}.tsx`
- Create: `client/src/components/home/__tests__/work-grid.test.tsx`
- Modify: `client/src/routes/home.tsx`

- [ ] **Step 1: Write the failing test for `WorkGrid`**

Create `client/src/components/home/__tests__/work-grid.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import WorkGrid from '@/components/home/work-grid';
import { seedProjects } from '@/content/seed-projects';

describe('<WorkGrid />', () => {
  it('renders a card for each seed project', () => {
    render(<WorkGrid />);
    seedProjects.forEach((p) => {
      expect(screen.getByText(p.name)).toBeInTheDocument();
      expect(screen.getByText(p.type)).toBeInTheDocument();
    });
  });

  it('exposes an anchor target for in-page nav (id="work")', () => {
    const { container } = render(<WorkGrid />);
    expect(container.querySelector('#work')).not.toBeNull();
  });

  it('renders all tags as Badges', () => {
    render(<WorkGrid />);
    seedProjects.forEach((p) => {
      p.tags.forEach((tag) => {
        // Tags can repeat across projects, so just assert at least one match exists.
        expect(screen.getAllByText(tag).length).toBeGreaterThan(0);
      });
    });
  });
});
```

- [ ] **Step 2: Run the test and verify it fails**

Run: `npm run test:run -- work-grid`
Expected: fails because `WorkGrid` doesn't exist.

- [ ] **Step 3: Create `client/src/components/home/work-grid.tsx`**

```tsx
import { Card, CardContent } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { seedProjects } from '@/content/seed-projects';
import type { Project } from '@/lib/types';

function ShapeBlock({ project }: { project: Project }) {
  const baseStyle: React.CSSProperties = {
    background: project.accent,
  };
  if (project.shape === 'circle') {
    return <div className="h-10 w-10" style={{ ...baseStyle, borderRadius: '50%' }} />;
  }
  if (project.shape === 'parallelogram') {
    return (
      <div
        className="h-10 w-10 rounded-md"
        style={{ ...baseStyle, transform: 'skewX(-12deg)' }}
      />
    );
  }
  return <div className="h-10 w-10 rounded-md" style={baseStyle} />;
}

export default function WorkGrid() {
  return (
    <section id="work" className="mb-6">
      <div className="mb-4 flex items-baseline justify-between px-1">
        <h2 className="text-lg font-medium text-blue-950">selected work</h2>
        <span className="text-xs text-muted-foreground">2024 — 2026</span>
      </div>
      <div className="grid grid-cols-1 gap-3 md:grid-cols-2">
        {seedProjects.map((p) => (
          <Card
            key={p.name}
            className="cursor-pointer transition-colors hover:border-blue-300"
          >
            <CardContent className="p-5">
              <div
                className="mb-3 flex h-24 items-center justify-center rounded-md"
                style={{ background: p.bg }}
              >
                <ShapeBlock project={p} />
              </div>
              <div className="flex items-start justify-between">
                <div>
                  <p className="text-sm font-medium text-blue-950">{p.name}</p>
                  <p className="text-xs text-muted-foreground">{p.type}</p>
                </div>
                <span className="text-xs text-muted-foreground">{p.year}</span>
              </div>
              <div className="mt-3 flex flex-wrap gap-1.5">
                {p.tags.map((t) => (
                  <Badge key={t} variant="outline" className="text-xs font-normal">
                    {t}
                  </Badge>
                ))}
              </div>
            </CardContent>
          </Card>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 4: Run the test and verify it passes**

Run: `npm run test:run -- work-grid`
Expected: all `<WorkGrid />` tests pass.

- [ ] **Step 5: Create `client/src/components/home/hero.tsx`**

```tsx
import { Link } from 'react-router-dom';
import { Card, CardContent } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { ArrowRight, Sparkles } from 'lucide-react';

export default function Hero() {
  return (
    <Card className="mb-6">
      <CardContent className="p-10">
        <Badge
          variant="secondary"
          className="mb-4 bg-blue-50 text-blue-900 hover:bg-blue-50"
        >
          <Sparkles className="mr-1.5 h-3 w-3" />
          available for work · 2026
        </Badge>
        <h1 className="mb-4 max-w-xl text-4xl font-medium leading-tight text-blue-950">
          Developer crafting calm, considered interfaces.
        </h1>
        <p className="mb-6 max-w-xl text-base leading-relaxed text-muted-foreground">
          I help small teams turn rough ideas into shipped products. I care about the
          quiet details: tight type, sensible defaults, and code that reads like prose.
        </p>
        <div className="flex gap-2.5">
          <Button asChild className="bg-blue-700 hover:bg-blue-800">
            <a href="#work">
              view work
              <ArrowRight className="ml-1.5 h-4 w-4" />
            </a>
          </Button>
          <Button asChild variant="outline">
            <Link to="/contact">get in touch</Link>
          </Button>
        </div>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 6: Create `client/src/components/home/stats.tsx`**

```tsx
import { Card, CardContent } from '@/components/ui/card';

const stats = [
  { label: 'projects shipped', value: '42' },
  { label: 'years building', value: '7' },
  { label: 'happy clients', value: '28' },
  { label: 'coffee per week', value: '∞' },
];

export default function Stats() {
  return (
    <div className="mb-6 grid grid-cols-2 gap-3 md:grid-cols-4">
      {stats.map((s) => (
        <Card key={s.label}>
          <CardContent className="p-4">
            <p className="mb-1 text-xs text-muted-foreground">{s.label}</p>
            <p className="text-2xl font-medium text-blue-950">{s.value}</p>
          </CardContent>
        </Card>
      ))}
    </div>
  );
}
```

- [ ] **Step 7: Create `client/src/components/home/tools-card.tsx`**

```tsx
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';

const tools = ['Figma', 'Framer', 'React', 'Tailwind', 'Notion', 'Linear', 'Webflow'];

export default function ToolsCard() {
  return (
    <Card className="mb-6">
      <CardHeader className="pb-3">
        <CardTitle className="text-base font-medium">tools & stack</CardTitle>
        <CardDescription className="text-xs">things I reach for daily</CardDescription>
      </CardHeader>
      <CardContent>
        <div className="flex flex-wrap gap-2">
          {tools.map((t) => (
            <Badge
              key={t}
              variant="secondary"
              className="bg-blue-50 text-blue-900 hover:bg-blue-100"
            >
              {t}
            </Badge>
          ))}
        </div>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 8: Create `client/src/components/home/contact-cta.tsx`**

```tsx
import { Link } from 'react-router-dom';
import { Card, CardContent } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { ArrowRight } from 'lucide-react';

export default function ContactCTA() {
  return (
    <Card>
      <CardContent className="flex items-center justify-between gap-4 p-6">
        <div>
          <p className="text-base font-medium text-blue-950">Have a project in mind?</p>
          <p className="text-sm text-muted-foreground">
            Booking from June 2026 · responses within 24h
          </p>
        </div>
        <Button asChild className="whitespace-nowrap bg-blue-700 hover:bg-blue-800">
          <Link to="/contact">
            say hello
            <ArrowRight className="ml-1.5 h-4 w-4" />
          </Link>
        </Button>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 9: Replace `client/src/routes/home.tsx`**

```tsx
import Hero from '@/components/home/hero';
import Stats from '@/components/home/stats';
import WorkGrid from '@/components/home/work-grid';
import ToolsCard from '@/components/home/tools-card';
import ContactCTA from '@/components/home/contact-cta';

export default function Home() {
  return (
    <>
      <Hero />
      <Stats />
      <WorkGrid />
      <ToolsCard />
      <ContactCTA />
    </>
  );
}
```

- [ ] **Step 10: Run all tests**

Run: `npm run test:run`
Expected: all tests pass.

- [ ] **Step 11: Manual visual check**

Run: `npm run dev`. Open `/`. Expected:
- Hero card with badge, headline, paragraph, two buttons. "view work" scrolls to the selected-work section. "get in touch" navigates to `/contact`.
- 4-card stats grid (2 cols mobile, 4 cols desktop).
- "selected work" section with 4 project cards (square/parallelogram/circle shapes match SPEC).
- "tools & stack" card with badges.
- Contact CTA card with "say hello" button → `/contact`.
- All visuals match `docs/Portfolio.jsx`.

Stop the server.

- [ ] **Step 12: Commit**

```bash
git add client/
git commit -m "feat(client): build homepage components"
```

---

## Task 8: Blogs list page (TDD)

**Files:**
- Create: `client/src/components/blog/post-card.tsx`, `client/src/components/blog/post-meta.tsx`, `client/src/components/blog/__tests__/post-card.test.tsx`, `client/src/routes/__tests__/blogs.test.tsx`
- Modify: `client/src/routes/blogs.tsx`

- [ ] **Step 1: Create `client/src/components/blog/post-meta.tsx`**

```tsx
import type { PostSummary } from '@/lib/types';

const fmtDate = (iso: string) =>
  new Date(iso).toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'short',
    day: 'numeric',
  });

export function PostMeta({ post }: { post: Pick<PostSummary, 'publishedAt' | 'readingMinutes'> }) {
  return (
    <p className="text-xs text-muted-foreground">
      {fmtDate(post.publishedAt)} · {post.readingMinutes} min read
    </p>
  );
}
```

- [ ] **Step 2: Write the failing test for `PostCard`**

Create `client/src/components/blog/__tests__/post-card.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import PostCard from '@/components/blog/post-card';
import type { PostSummary } from '@/lib/types';

const sample: PostSummary = {
  slug: 'hello',
  title: 'Hello world',
  excerpt: 'A first post.',
  publishedAt: '2026-04-01',
  readingMinutes: 3,
  tags: ['intro', 'meta'],
};

function renderCard() {
  return render(
    <MemoryRouter>
      <PostCard post={sample} />
    </MemoryRouter>
  );
}

describe('<PostCard />', () => {
  it('renders title, excerpt, date, reading time, and tags', () => {
    renderCard();
    expect(screen.getByText('Hello world')).toBeInTheDocument();
    expect(screen.getByText('A first post.')).toBeInTheDocument();
    expect(screen.getByText(/3 min read/)).toBeInTheDocument();
    expect(screen.getByText('intro')).toBeInTheDocument();
    expect(screen.getByText('meta')).toBeInTheDocument();
  });

  it('links to /blogs/<slug>', () => {
    renderCard();
    const link = screen.getByRole('link', { name: /Hello world/i });
    expect(link).toHaveAttribute('href', '/blogs/hello');
  });
});
```

- [ ] **Step 3: Run the test and verify it fails**

Run: `npm run test:run -- post-card`
Expected: fails because `PostCard` doesn't exist.

- [ ] **Step 4: Create `client/src/components/blog/post-card.tsx`**

```tsx
import { Link } from 'react-router-dom';
import { Card, CardContent } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { PostMeta } from '@/components/blog/post-meta';
import type { PostSummary } from '@/lib/types';

export default function PostCard({ post }: { post: PostSummary }) {
  return (
    <Link to={`/blogs/${post.slug}`} className="block">
      <Card className="cursor-pointer transition-colors hover:border-blue-300">
        <CardContent className="p-5">
          <h3 className="mb-1 text-base font-medium text-blue-950">{post.title}</h3>
          <p className="mb-3 line-clamp-2 text-sm text-muted-foreground">{post.excerpt}</p>
          <div className="flex items-center justify-between">
            <PostMeta post={post} />
            <div className="flex flex-wrap gap-1.5">
              {post.tags.map((t) => (
                <Badge key={t} variant="outline" className="text-xs font-normal">
                  {t}
                </Badge>
              ))}
            </div>
          </div>
        </CardContent>
      </Card>
    </Link>
  );
}
```

- [ ] **Step 5: Run the test and verify it passes**

Run: `npm run test:run -- post-card`
Expected: all `<PostCard />` tests pass.

- [ ] **Step 6: Write the failing test for the `Blogs` route**

Create `client/src/routes/__tests__/blogs.test.tsx`:

```tsx
import { render, screen, waitFor } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import Blogs from '@/routes/blogs';
import { seedPosts } from '@/content/seed-posts';

function renderBlogs() {
  return render(
    <MemoryRouter>
      <Blogs />
    </MemoryRouter>
  );
}

describe('<Blogs />', () => {
  it('shows a loading state initially', () => {
    renderBlogs();
    expect(screen.getByTestId('blogs-loading')).toBeInTheDocument();
  });

  it('renders a card for every post once loaded', async () => {
    renderBlogs();
    for (const p of seedPosts) {
      await waitFor(() => expect(screen.getByText(p.title)).toBeInTheDocument());
    }
  });
});
```

- [ ] **Step 7: Run the test and verify it fails**

Run: `npm run test:run -- blogs`
Expected: fails — current `blogs.tsx` is a placeholder.

- [ ] **Step 8: Replace `client/src/routes/blogs.tsx`**

```tsx
import { useEffect, useState } from 'react';
import { api } from '@/lib/api';
import type { ApiError, PostSummary } from '@/lib/types';
import { Skeleton } from '@/components/ui/skeleton';
import { Card, CardContent } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import PostCard from '@/components/blog/post-card';

type State =
  | { status: 'loading' }
  | { status: 'ok'; posts: PostSummary[] }
  | { status: 'error'; message: string };

export default function Blogs() {
  const [state, setState] = useState<State>({ status: 'loading' });

  const load = () => {
    setState({ status: 'loading' });
    api.posts
      .list()
      .then((posts) => setState({ status: 'ok', posts }))
      .catch((e: ApiError) =>
        setState({ status: 'error', message: e.message ?? 'Failed to load posts' })
      );
  };

  useEffect(() => {
    load();
  }, []);

  return (
    <section>
      <div className="mb-4 flex items-baseline justify-between px-1">
        <h2 className="text-lg font-medium text-blue-950">writing</h2>
        <span className="text-xs text-muted-foreground">2026</span>
      </div>

      {state.status === 'loading' && (
        <div data-testid="blogs-loading" className="flex flex-col gap-3">
          {[0, 1, 2].map((i) => (
            <Card key={i}>
              <CardContent className="p-5">
                <Skeleton className="mb-2 h-5 w-2/3" />
                <Skeleton className="mb-4 h-4 w-full" />
                <Skeleton className="h-3 w-1/3" />
              </CardContent>
            </Card>
          ))}
        </div>
      )}

      {state.status === 'error' && (
        <Card>
          <CardContent className="flex flex-col items-start gap-3 p-5">
            <p className="text-sm text-muted-foreground">{state.message}</p>
            <Button variant="outline" onClick={load}>
              retry
            </Button>
          </CardContent>
        </Card>
      )}

      {state.status === 'ok' && state.posts.length === 0 && (
        <Card>
          <CardContent className="p-5">
            <p className="text-sm text-muted-foreground">
              No posts yet — check back soon.
            </p>
          </CardContent>
        </Card>
      )}

      {state.status === 'ok' && state.posts.length > 0 && (
        <div className="flex flex-col gap-3">
          {state.posts.map((p) => (
            <PostCard key={p.slug} post={p} />
          ))}
        </div>
      )}
    </section>
  );
}
```

- [ ] **Step 9: Run the tests and verify they pass**

Run: `npm run test:run -- blogs`
Expected: both `<Blogs />` tests pass.

- [ ] **Step 10: Manual check**

Run: `npm run dev`, open `/blogs`. Expected:
- Brief skeleton flash, then 4 post cards.
- Clicking a card navigates to `/blogs/<slug>` (still placeholder until Task 9).

Stop the server.

- [ ] **Step 11: Commit**

```bash
git add client/
git commit -m "feat(client): build blogs list page"
```

---

## Task 9: Blog detail page with markdown rendering (TDD)

**Files:**
- Create: `client/src/routes/__tests__/blog-detail.test.tsx`
- Modify: `client/src/routes/blog-detail.tsx`, `client/package.json`, `client/src/globals.css`

- [ ] **Step 1: Install markdown deps**

Run:
```bash
npm install react-markdown remark-gfm
npm install -D @tailwindcss/typography
```

- [ ] **Step 2: Enable typography plugin in globals.css**

At the top of `client/src/globals.css`, just below `@import "tailwindcss";`, add:

```css
@plugin "@tailwindcss/typography";
```

(Tailwind v4 loads plugins via the `@plugin` directive in CSS, not a JS config.)

- [ ] **Step 3: Write the failing test for `BlogDetail`**

Create `client/src/routes/__tests__/blog-detail.test.tsx`:

```tsx
import { render, screen, waitFor } from '@testing-library/react';
import { MemoryRouter, Route, Routes } from 'react-router-dom';
import BlogDetail from '@/routes/blog-detail';

function renderAt(path: string) {
  return render(
    <MemoryRouter initialEntries={[path]}>
      <Routes>
        <Route path="/blogs/:slug" element={<BlogDetail />} />
      </Routes>
    </MemoryRouter>
  );
}

describe('<BlogDetail />', () => {
  it('renders the post title and markdown body when slug exists', async () => {
    renderAt('/blogs/calm-interfaces');
    await waitFor(() =>
      expect(screen.getByText('On building calm interfaces')).toBeInTheDocument()
    );
    expect(screen.getByText(/Fewer colors/)).toBeInTheDocument();
  });

  it('renders a not-found state when slug does not exist', async () => {
    renderAt('/blogs/nope');
    await waitFor(() => expect(screen.getByText(/Post not found/i)).toBeInTheDocument());
  });
});
```

- [ ] **Step 4: Run the test and verify it fails**

Run: `npm run test:run -- blog-detail`
Expected: fails — current `blog-detail.tsx` is a placeholder.

- [ ] **Step 5: Replace `client/src/routes/blog-detail.tsx`**

```tsx
import { useEffect, useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import ReactMarkdown from 'react-markdown';
import remarkGfm from 'remark-gfm';
import { api } from '@/lib/api';
import type { ApiError, Post } from '@/lib/types';
import { Card, CardContent } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { Separator } from '@/components/ui/separator';
import { Skeleton } from '@/components/ui/skeleton';
import { ArrowLeft } from 'lucide-react';
import { PostMeta } from '@/components/blog/post-meta';

type State =
  | { status: 'loading' }
  | { status: 'ok'; post: Post }
  | { status: 'not-found' }
  | { status: 'error'; message: string };

export default function BlogDetail() {
  const { slug } = useParams<{ slug: string }>();
  const [state, setState] = useState<State>({ status: 'loading' });

  useEffect(() => {
    if (!slug) {
      setState({ status: 'not-found' });
      return;
    }
    setState({ status: 'loading' });
    api.posts
      .get(slug)
      .then((post) =>
        post ? setState({ status: 'ok', post }) : setState({ status: 'not-found' })
      )
      .catch((e: ApiError) =>
        setState({ status: 'error', message: e.message ?? 'Failed to load post' })
      );
  }, [slug]);

  return (
    <article>
      <Button asChild variant="ghost" size="sm" className="mb-4 text-blue-700">
        <Link to="/blogs">
          <ArrowLeft className="mr-1.5 h-4 w-4" />
          back to writing
        </Link>
      </Button>

      <Card>
        <CardContent className="p-8">
          {state.status === 'loading' && (
            <div>
              <Skeleton className="mb-4 h-8 w-3/4" />
              <Skeleton className="mb-2 h-4 w-1/3" />
              <Separator className="my-6" />
              {[0, 1, 2, 3].map((i) => (
                <Skeleton key={i} className="mb-2 h-4 w-full" />
              ))}
            </div>
          )}

          {state.status === 'not-found' && (
            <div className="flex flex-col items-start gap-3">
              <p className="text-sm text-muted-foreground">Post not found.</p>
              <Button asChild variant="outline">
                <Link to="/blogs">back to writing</Link>
              </Button>
            </div>
          )}

          {state.status === 'error' && (
            <p className="text-sm text-muted-foreground">{state.message}</p>
          )}

          {state.status === 'ok' && (
            <>
              <h1 className="text-3xl font-medium text-blue-950">{state.post.title}</h1>
              <div className="mt-2 flex flex-wrap items-center gap-3">
                <PostMeta post={state.post} />
                <div className="flex flex-wrap gap-1.5">
                  {state.post.tags.map((t) => (
                    <Badge key={t} variant="outline" className="text-xs font-normal">
                      {t}
                    </Badge>
                  ))}
                </div>
              </div>
              <Separator className="my-6" />
              <div className="prose prose-sm prose-blue max-w-none prose-headings:text-blue-950 prose-a:text-blue-700">
                <ReactMarkdown remarkPlugins={[remarkGfm]}>
                  {state.post.content}
                </ReactMarkdown>
              </div>
            </>
          )}
        </CardContent>
      </Card>
    </article>
  );
}
```

- [ ] **Step 6: Run the test and verify it passes**

Run: `npm run test:run -- blog-detail`
Expected: both `<BlogDetail />` tests pass.

- [ ] **Step 7: Manual check**

Run: `npm run dev`. Visit `/blogs/calm-interfaces`:
- Title in `text-blue-950`, meta + tags below.
- Markdown body renders with `prose` styling: H1/H2/lists.
- "back to writing" button returns to `/blogs`.
- Visit `/blogs/no-such-post` → "Post not found" + back link.

Stop the server.

- [ ] **Step 8: Commit**

```bash
git add client/
git commit -m "feat(client): build blog detail page with markdown rendering"
```

---

## Task 10: Contact page (TDD)

**Files:**
- Create: `client/src/routes/__tests__/contact.test.tsx`
- Modify: `client/src/routes/contact.tsx`, `client/src/App.tsx` (mount Toaster)

- [ ] **Step 1: Mount the `Toaster` in `App.tsx`**

Update `client/src/App.tsx` to include the sonner Toaster (added globally so any route can use `toast.success`/`toast.error`):

```tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { Toaster } from '@/components/ui/sonner';
import Nav from '@/components/layout/nav';
import Footer from '@/components/layout/footer';
import Home from '@/routes/home';
import Blogs from '@/routes/blogs';
import BlogDetail from '@/routes/blog-detail';
import Contact from '@/routes/contact';
import NotFound from '@/routes/not-found';

export default function App() {
  return (
    <BrowserRouter>
      <Nav />
      <main className="mx-auto max-w-5xl px-8 pb-16">
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/blogs" element={<Blogs />} />
          <Route path="/blogs/:slug" element={<BlogDetail />} />
          <Route path="/contact" element={<Contact />} />
          <Route path="*" element={<NotFound />} />
        </Routes>
      </main>
      <Footer />
      <Toaster />
    </BrowserRouter>
  );
}
```

- [ ] **Step 2: Write the failing test for the `Contact` page**

Create `client/src/routes/__tests__/contact.test.tsx`:

```tsx
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { MemoryRouter } from 'react-router-dom';
import Contact from '@/routes/contact';

function renderContact() {
  return render(
    <MemoryRouter>
      <Contact />
    </MemoryRouter>
  );
}

describe('<Contact />', () => {
  it('renders the form fields', () => {
    renderContact();
    expect(screen.getByLabelText(/name/i)).toBeInTheDocument();
    expect(screen.getByLabelText(/email/i)).toBeInTheDocument();
    expect(screen.getByLabelText(/message/i)).toBeInTheDocument();
    expect(screen.getByRole('button', { name: /send message/i })).toBeInTheDocument();
  });

  it('shows a validation message when submitting an empty form', async () => {
    const user = userEvent.setup();
    renderContact();
    await user.click(screen.getByRole('button', { name: /send message/i }));
    await waitFor(() =>
      expect(screen.getByText(/please fill in all fields/i)).toBeInTheDocument()
    );
  });

  it('clears the form after a successful submit', async () => {
    const user = userEvent.setup();
    renderContact();
    await user.type(screen.getByLabelText(/name/i), 'Hieu');
    await user.type(screen.getByLabelText(/email/i), 'hi@hieu.dev');
    await user.type(
      screen.getByLabelText(/message/i),
      'This is a test message that is long enough.'
    );
    await user.click(screen.getByRole('button', { name: /send message/i }));

    await waitFor(() =>
      expect((screen.getByLabelText(/name/i) as HTMLInputElement).value).toBe('')
    );
  });
});
```

- [ ] **Step 3: Run the test and verify it fails**

Run: `npm run test:run -- contact`
Expected: fails — current `contact.tsx` is a placeholder.

- [ ] **Step 4: Replace `client/src/routes/contact.tsx`**

```tsx
import { useState } from 'react';
import { toast } from 'sonner';
import { Card, CardContent } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Textarea } from '@/components/ui/textarea';
import { Separator } from '@/components/ui/separator';
import { ArrowRight, Github, Mail, Twitter } from 'lucide-react';
import { api } from '@/lib/api';
import type { ApiError } from '@/lib/types';

type FormState = { name: string; email: string; message: string };
const empty: FormState = { name: '', email: '', message: '' };

export default function Contact() {
  const [form, setForm] = useState<FormState>(empty);
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const update = (field: keyof FormState) =>
    (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) =>
      setForm((f) => ({ ...f, [field]: e.target.value }));

  const onSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setError(null);

    if (!form.name.trim() || !form.email.trim() || form.message.trim().length < 10) {
      setError(
        'Please fill in all fields. Message must be at least 10 characters.'
      );
      return;
    }

    setSubmitting(true);
    try {
      await api.contact.send(form);
      toast.success('Thanks — message received.');
      setForm(empty);
    } catch (err) {
      const msg = (err as ApiError).message ?? 'Something went wrong.';
      toast.error(`${msg} Try again or email me directly.`);
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <Card>
      <CardContent className="p-8">
        <h1 className="text-3xl font-medium text-blue-950">say hello</h1>
        <p className="mt-2 text-sm text-muted-foreground">
          I'll get back within 24h. Booking from June 2026.
        </p>

        <form onSubmit={onSubmit} className="mt-6 flex flex-col gap-4">
          <div className="flex flex-col gap-1.5">
            <label htmlFor="name" className="text-sm font-medium text-blue-950">
              name
            </label>
            <Input
              id="name"
              value={form.name}
              onChange={update('name')}
              autoComplete="name"
            />
          </div>
          <div className="flex flex-col gap-1.5">
            <label htmlFor="email" className="text-sm font-medium text-blue-950">
              email
            </label>
            <Input
              id="email"
              type="email"
              value={form.email}
              onChange={update('email')}
              autoComplete="email"
            />
          </div>
          <div className="flex flex-col gap-1.5">
            <label htmlFor="message" className="text-sm font-medium text-blue-950">
              message
            </label>
            <Textarea
              id="message"
              value={form.message}
              onChange={update('message')}
              rows={6}
            />
          </div>

          {error && <p className="text-sm text-red-600">{error}</p>}

          <div>
            <Button
              type="submit"
              disabled={submitting}
              className="bg-blue-700 hover:bg-blue-800"
            >
              {submitting ? 'sending…' : 'send message'}
              {!submitting && <ArrowRight className="ml-1.5 h-4 w-4" />}
            </Button>
          </div>
        </form>

        <Separator className="my-6" />

        <div className="flex items-center gap-3">
          <p className="text-sm text-muted-foreground">Or reach me at</p>
          <Button asChild variant="outline" size="icon" aria-label="email">
            <a href="mailto:hi@hieu.dev">
              <Mail className="h-4 w-4" />
            </a>
          </Button>
          <Button asChild variant="outline" size="icon" aria-label="github">
            <a href="https://github.com/" target="_blank" rel="noreferrer">
              <Github className="h-4 w-4" />
            </a>
          </Button>
          <Button asChild variant="outline" size="icon" aria-label="twitter">
            <a href="https://twitter.com/" target="_blank" rel="noreferrer">
              <Twitter className="h-4 w-4" />
            </a>
          </Button>
        </div>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 5: Run the test and verify it passes**

Run: `npm run test:run -- contact`
Expected: all `<Contact />` tests pass.

- [ ] **Step 6: Run the entire test suite**

Run: `npm run test:run`
Expected: all tests across all files pass.

- [ ] **Step 7: Manual check**

Run: `npm run dev`, open `/contact`:
- Form renders. Submitting empty shows the inline validation error.
- Filling name/email/long-enough message and clicking "send message" shows a green success toast and clears the form.
- Three icon buttons in the bottom row (mail/github/twitter).

Stop the server.

- [ ] **Step 8: Commit**

```bash
git add client/
git commit -m "feat(client): build contact page with mock submission and toast"
```

---

## Task 11: End-to-end manual smoke check + responsive sweep

**Files:** none modified — verification only

- [ ] **Step 1: Run all tests one final time**

Run: `npm run test:run`
Expected: all passing.

- [ ] **Step 2: Run a production build**

Run: `npm run build`
Expected: succeeds, no TypeScript errors.

- [ ] **Step 3: Preview the production build**

Run: `npm run preview`
Expected: opens at the printed local URL. The site behaves identically to dev.

- [ ] **Step 4: Walk every route**

Visit each path and verify:
- `/` — hero, stats, work grid, tools, contact CTA. "view work" anchors to the work section. "get in touch" → `/contact`. "say hello" → `/contact`.
- `/blogs` — 4 post cards (newest first), clicking one opens detail.
- `/blogs/calm-interfaces` — title, meta, prose body, "back to writing".
- `/blogs/no-such-slug` — "Post not found".
- `/contact` — form submit success and validation error.
- `/asdfgh` — 404 card with "back home".
- Nav active link underline appears on the right route.
- Dot grid stays continuous across all navigations.

- [ ] **Step 5: Responsive sweep**

In DevTools, resize to:
- 375px (iPhone SE) — stats grid is 2 cols, post cards full width, nav fits, no horizontal scroll.
- 768px (iPad) — stats grid is 4 cols, work grid is 2 cols.
- 1024px+ (desktop) — full layout per SPEC.

If any layout breaks at 375px, fix in place (commonly: reduce `p-10` to `p-6` on hero, ensure `flex-wrap` on the contact CTA top row).

- [ ] **Step 6: Final commit (if any tweaks were made)**

```bash
git add client/
git commit -m "chore(client): responsive polish"
```

If no changes were needed, skip this step.

---

## Done

The frontend phase is complete. The `api` module is the only seam that needs swapping when the Koa+Firebase backend lands. Setting `VITE_API_BASE` will route every page through the real backend without component changes.
